# 토픽 메시지 흐름 상세 가이드

## 🎯 핵심 질문

**Q: 토픽에는 집계 데이터가 쌓이고 있던데, 윈도우 닫았을 때만 리스너가 받을 수 있다는 거야?**

**A: 네, 정확합니다!** 하지만 **어떤 토픽**인지가 중요합니다.

## 📊 3개의 저장소 구분

```
┌──────────────────────────────────────────────────────────┐
│ 1. Input Topic (hourly-sales-topic)                     │
│    - 원본 데이터가 계속 쌓임 ✅                           │
│    - 영구 보관 (retention 설정에 따라)                    │
│    - 스트림즈가 읽어가지만 삭제 안 됨                      │
└──────────────────────────────────────────────────────────┘
              │
              │ 스트림즈가 계속 consume
              ▼
┌──────────────────────────────────────────────────────────┐
│ 2. 스트림즈 내부 상태 저장소 (RocksDB)                    │
│    - 토픽이 아님! ❌                                      │
│    - 로컬 디스크/메모리                                   │
│    - 윈도우별 집계 중인 데이터                             │
│    - 윈도우 닫히면 삭제됨                                 │
└──────────────────────────────────────────────────────────┘
              │
              │ 윈도우 닫힐 때만!
              ▼
┌──────────────────────────────────────────────────────────┐
│ 3. Output Topic (hourly-sales-output-topic)              │
│    - 최종 집계 결과만 저장 ✅                             │
│    - 윈도우 닫힐 때만 메시지 추가됨                        │
│    - 리스너는 이것만 읽음                                  │
└──────────────────────────────────────────────────────────┘
```

## 🕐 실제 시간별 흐름

### 14:00 ~ 15:00 윈도우 예시

```
시간      Input Topic              스트림즈 내부           Output Topic         리스너
────────────────────────────────────────────────────────────────────────────────────
14:05    [메시지1: 150만원] ✅      누적: 150만원          (비어있음)           (받는 메시지 없음)
         저장됨!

14:30    [메시지1: 150만원] ✅      누적: 330만원          (비어있음)           (받는 메시지 없음)
         [메시지2: 180만원] ✅
         둘 다 저장됨!

14:50    [메시지1: 150만원] ✅      누적: 530만원          (비어있음)           (받는 메시지 없음)
         [메시지2: 180만원] ✅
         [메시지3: 200만원] ✅
         셋 다 저장됨!

15:00    (윈도우 종료 시간)
         아직 닫히지 않음!

15:05    [메시지1: 150만원] ✅      15시 윈도우 시작       [결과: 530만원] ✅   ✅ 메시지 수신!
         [메시지2: 180만원] ✅      14시 상태 삭제         추가됨!              → DB 저장!
         [메시지3: 200만원] ✅
         [메시지4: 100만원] ✅
         네 개 모두 저장!
```

## 🔍 각 토픽의 상태 확인

### 1. Input Topic (hourly-sales-topic)

**특징:**
- 원본 데이터가 **계속 누적**됨
- 메시지는 **삭제되지 않음** (retention 기간까지)
- 여러 컨슈머가 읽을 수 있음

**실제 메시지:**
```bash
# Kafka 명령어로 확인
kafka-console-consumer --bootstrap-server localhost:9092 \
  --topic hourly-sales-topic \
  --from-beginning

# 출력 (모든 원본 데이터)
노트북	1500000
노트북	1800000
노트북	2000000
마우스	50000
마우스	80000
...
```

### 2. 스트림즈 내부 상태 (RocksDB)

**특징:**
- 토픽이 **아님**! 로컬 저장소
- `application_id/state-store-name/` 디렉토리에 저장
- 윈도우별로 집계 중인 상태

**저장 위치:**
```bash
/tmp/kafka-streams/kafka-streams-app/0_0/
```

**내용 (개념적):**
```
Window[14:00~15:00] → 노트북: 5300000원
Window[14:00~15:00] → 마우스: 130000원
Window[15:00~16:00] → 노트북: 1000000원 (집계 중)
```

### 3. Output Topic (hourly-sales-output-topic)

**특징:**
- **윈도우가 닫힐 때만** 메시지 추가
- 최종 집계 결과만 저장
- 리스너는 이것만 읽음

**실제 메시지:**
```bash
# Kafka 명령어로 확인
kafka-console-consumer --bootstrap-server localhost:9092 \
  --topic hourly-sales-output-topic \
  --from-beginning

# 출력 (윈도우 닫힐 때만!)
노트북	상품:노트북, 시간대:14:00~15:00, 총매출:5300000
마우스	상품:마우스, 시간대:14:00~15:00, 총매출:130000
키보드	상품:키보드, 시간대:14:00~15:00, 총매출:270000
노트북	상품:노트북, 시간대:15:00~16:00, 총매출:10000000
...
```

## 💡 왜 리스너는 윈도우 닫힐 때만 받나?

### 코드 흐름

```java
// KafkaStreamsConfig.java
stream
    .groupByKey()
    .windowedBy(TimeWindows.ofSizeWithNoGrace(Duration.ofHours(1)))
    .aggregate(...)  // ← 여기서 계속 집계 (내부 상태만)
    .toStream()      // ← 윈도우 닫히면 KStream으로 변환
    .map(...)        // ← 결과 포맷팅
    .to("hourly-sales-output-topic");  // ← 이때 Output 토픽에 전송!
                                        //    (윈도우 닫힐 때만!)
```

**핵심:**
- `.aggregate()`는 **내부 상태만** 업데이트
- `.to()`는 **Output 토픽**에 전송
- 윈도우가 열려있는 동안은 `.to()`가 실행 안 됨!

### 리스너 코드

```java
// TimeWindowConsumer.java
@KafkaListener(topics = "hourly-sales-output-topic")  // ← Output 토픽만 구독
public void listen(String message) {
    // Output 토픽에 메시지가 올 때만 실행
    // = 윈도우가 닫혔을 때만 실행
    storageService.save(message);
}
```

## 🔄 메시지 흐름 비교

### 일반 토픽 (실시간)

```
Producer → Topic → Consumer
           ↓
      즉시 전달! ✅
```

**예시:**
```java
// 프로듀서
kafkaTemplate.send("topic", "메시지");

// 컨슈머 (즉시 받음!)
@KafkaListener(topics = "topic")
void listen(String msg) { }
```

### 윈도우 집계 토픽 (지연)

```
Producer → Input Topic → Streams → Output Topic → Consumer
                          (집계)        ↓
                          (내부)    윈도우 닫힐 때만! ⏰
```

**예시:**
```java
// 14:05 프로듀서
kafkaTemplate.send("input-topic", "150만원");
// 컨슈머: 받지 못함 ❌

// 14:30 프로듀서
kafkaTemplate.send("input-topic", "180만원");
// 컨슈머: 받지 못함 ❌

// 15:05 프로듀서 (새 윈도우)
kafkaTemplate.send("input-topic", "100만원");
// 이 시점에 14시 윈도우 닫힘
// 컨슈머: "330만원" 받음! ✅
```

## 🎯 실습으로 확인하기

### 1단계: Kafka Console Consumer로 모니터링

**터미널 1: Output 토픽 모니터링**
```bash
kafka-console-consumer --bootstrap-server localhost:9092 \
  --topic hourly-sales-output-topic

# 처음엔 아무것도 안 나옴
# 윈도우 닫힐 때 갑자기 메시지 나타남!
```

**터미널 2: 애플리케이션 실행**
```bash
./gradlew bootRun
```

**터미널 3: 데이터 입력**
```bash
# 첫 번째 데이터
curl -X POST "http://localhost:8080/api/time-window/hourly-sales?product=테스트&amount=1000000"
# → 터미널 1: 아무 변화 없음

# 두 번째 데이터
curl -X POST "http://localhost:8080/api/time-window/hourly-sales?product=테스트&amount=2000000"
# → 터미널 1: 아무 변화 없음

# 한 시간 후... 또는 다음 윈도우 데이터 입력
# → 터미널 1: 갑자기 메시지 나타남! "3000000"
```

### 2단계: 로그로 확인

```
# 데이터 입력 시
[스트림-시간별집계] 입력 - 상품: 테스트, 금액: 1000000
[스트림-시간별집계] 상품: 테스트, 금액: 1000000, 누적: 0 -> 1000000
← Output 토픽에 전송 안 함!
← 리스너 실행 안 됨!

[스트림-시간별집계] 입력 - 상품: 테스트, 금액: 2000000
[스트림-시간별집계] 상품: 테스트, 금액: 2000000, 누적: 1000000 -> 3000000
← Output 토픽에 전송 안 함!
← 리스너 실행 안 됨!

# 한 시간 후...
✅ [시간별집계 결과] 상품:테스트, 시간대:14:00~15:00, 총매출:3000000
🕐 [시간별 집계 최종결과] Topic: hourly-sales-output-topic, Offset: 0, 결과: ...
💾 [DB 저장 완료] 시간별 매출 - 상품: 테스트, 매출: 3000000원
← 이때 비로소 리스너 실행!
```

## ⚠️ 흔한 오해

### 오해 1: "Input 토픽의 메시지가 삭제되나?"

❌ **아니오!** Input 토픽의 메시지는 **삭제되지 않습니다.**

```
Input Topic (영구 보관):
[14:05] 150만원
[14:30] 180만원
[14:50] 200만원
[15:05] 100만원  ← 새로 추가됨
↑ 모두 남아있음!
```

### 오해 2: "스트림즈가 Output 토픽에 계속 쓰고 있는 거 아냐?"

❌ **아니오!** 윈도우 닫힐 때만 씁니다.

```
Output Topic:
(비어있음)
(비어있음)
(비어있음)
[15:05] 530만원 ← 갑자기 추가됨! (윈도우 닫힘)
```

### 오해 3: "리스너가 계속 폴링하다가 받는 건가?"

✅ **맞습니다!** 하지만 메시지가 없으면 받을 게 없어요.

```java
@KafkaListener(topics = "hourly-sales-output-topic")
void listen(String msg) {
    // 이 메서드는 메시지가 토픽에 있을 때만 실행됨
    // 메시지가 없으면 실행 안 됨 (대기 중)
}
```

## 📊 메시지 개수 비교

### 3시간 동안 운영했을 때

**Input Topic (hourly-sales-topic):**
```
총 100개 메시지
- 14:00~15:00: 30개
- 15:00~16:00: 40개
- 16:00~17:00: 30개
```

**스트림즈 내부 상태:**
```
현재 집계 중인 윈도우만 유지
- 16:00~17:00: 누적 1500만원 (메모리에만)
```

**Output Topic (hourly-sales-output-topic):**
```
총 2개 메시지 (윈도우 닫힌 것만!)
- [15:05] 14:00~15:00 결과: 800만원
- [16:05] 15:00~16:00 결과: 1200만원
```

**리스너가 받은 메시지:**
```
총 2개 (Output 토픽과 동일)
- [15:05] 800만원 → DB 저장
- [16:05] 1200만원 → DB 저장
```

## 🎓 정리

### 핵심 포인트

1. **Input 토픽**: 원본 데이터가 **계속 쌓임** ✅
2. **스트림즈 내부**: 집계 중인 상태 (토픽 아님!)
3. **Output 토픽**: 윈도우 닫힐 때만 메시지 추가 ⏰
4. **리스너**: Output 토픽의 메시지만 받음 ✅

### 질문에 대한 최종 답변

**Q: 토픽에는 집계 데이터가 쌓이고 있던데, 윈도우 닫았을 때만 리스너가 받을 수 있다는 거야?**

**A: 정확합니다!** ✅

- **Input 토픽**에는 원본 데이터가 계속 쌓임
- **스트림즈 내부**에서 집계 중 (토픽 아님)
- **Output 토픽**에는 윈도우 닫힐 때만 결과가 전송됨
- **리스너**는 Output 토픽의 메시지만 받으므로, 윈도우 닫힐 때만 실행됨

이것이 바로 **배치 집계**와의 차이점이자, **실시간 스트림 처리**의 특징입니다!

## 🔍 디버깅 팁

### Output 토픽 직접 확인하기

```bash
# 메시지 개수 확인
kafka-run-class kafka.tools.GetOffsetShell \
  --broker-list localhost:9092 \
  --topic hourly-sales-output-topic

# 실시간 모니터링
kafka-console-consumer --bootstrap-server localhost:9092 \
  --topic hourly-sales-output-topic \
  --property print.timestamp=true \
  --property print.key=true

# 윈도우 닫힐 때만 메시지가 나타나는 것을 확인할 수 있습니다!
```

